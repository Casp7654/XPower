echo -e "Running..."
./bin/surreal start \
    --log debug \
    --user root \
    --pass root \
    --bind 127.0.0.1:8000 \
    file://surreal/store/CUR \
    || echo -e "\$ERR!" & \
    sleep 1 && ./dbtest